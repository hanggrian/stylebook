extends: default

rules:
  document-start: disable
  empty-lines:
    max: 1
  empty-values: enable
  indentation:
    spaces: 2
    indent-sequences: whatever
  float-values: enable
  octal-values: enable
  truthy:
    check-keys: false
